<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>{{basics.name}} - Resume</title>
  <style>
    {{> baseStyles}}
    body {
      --accent: {{accentColor}};
      --subtitle-weight: {{subtitleWeight}};
      --subtitle-style: {{subtitleFontStyle}};
      font-family: {{fontFamily}}, 'Helvetica Neue', Arial, sans-serif;
      color: #1f2937;
      margin: 0;
      padding: 30px 40px;
      font-size: {{fontSize}}px;
      line-height: 1.5;
    }
    header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 16px; }
    .photo { width: 60px; height: 60px; border-radius: 50%; object-fit: cover; }
    .photo.photo-square { border-radius: 8px; }
    h1 { font-size: 19px; margin: 0 0 2px; font-weight: 700; }
    h1.uppercase { text-transform: uppercase; }
    .label { font-size: 11.5px; color: var(--accent); margin: 0 0 8px; font-weight: 600; }
    .contact { font-size: 9.5px; color: #4b5563; }
    .contact span { margin-right: 12px; }
    section { margin-bottom: 16px; }
    /* Bold black rule under each heading and between entries (design reference: screenshot) — bolder than the
       other templates' thin gray section rules. */
    h2 {
      font-size: 11px; text-transform: uppercase; letter-spacing: 0.06em;
      color: #111827; border-bottom: 2px solid #111827; padding-bottom: 5px; margin: 0 0 10px; font-weight: 800;
    }
    .item { padding-bottom: 8px; margin-bottom: 8px; border-bottom: 1px solid #e5e7eb; }
    .item:last-child { border-bottom: none; margin-bottom: 0; padding-bottom: 0; }
    .item-title { font-weight: 700; font-size: 11px; }
    .item-subtitle { font-size: 10px; }
    .item-dates { float: right; font-size: 9.5px; color: #6b7280; }
    .tags span { background: #f3f4f6; color: #374151; }
    .skill-tags { display: flex; flex-wrap: wrap; gap: 4px 10px; }
    /* "Strengths"/"Key Achievements"-style entries render as a plain 2-column
       grid of bold-title + description, no icon avatar (design reference: screenshot). */
    section:has(> .icon-item), section:has(> .achievement-item) {
      display: grid; grid-template-columns: 1fr 1fr; column-gap: 24px; row-gap: 4px;
    }
    section:has(> .icon-item) h2, section:has(> .achievement-item) h2 { grid-column: 1 / -1; }
    .icon-item, .achievement-item { border: none; padding: 0; border-radius: 0; margin-bottom: 10px; }
    .icon-item .icon-avatar, .achievement-item .icon-avatar { display: none; }
  </style>
</head>
<body>
  <header>
    <div>
      <h1 class="{{#if profileSettings.uppercaseName}}uppercase{{/if}}">{{basics.name}}</h1>
      {{#if profileSettings.showHeadline}}<p class="label">{{basics.headline}}</p>{{/if}}
      <p class="contact">
        {{#if profileSettings.showPhone}}{{#if basics.phone}}<span>{{icon "phone"}}{{basics.phone}}</span>{{/if}}{{/if}}
        {{#if profileSettings.showEmail}}{{#if basics.email}}<span>{{icon "email"}}{{basics.email}}</span>{{/if}}{{/if}}
        {{#if profileSettings.showWebsite}}{{#if basics.website.url}}<span>{{icon "link"}}{{basics.website.url}}</span>{{/if}}{{/if}}
        {{#if profileSettings.showLocation}}{{#if basics.location}}<span>{{icon "location"}}{{basics.location}}</span>{{/if}}{{/if}}
      </p>
    </div>
    {{#unless picture.hidden}}{{#if picture.url}}
      <img class="photo {{#if (eq profileSettings.photoStyle "square")}}photo-square{{/if}}" src="{{picture.url}}" />
    {{/if}}{{/unless}}
  </header>

  {{#unless summary.hidden}}{{#if summary.content.length}}
  <section>
    <h2>{{icon "summary"}}{{summary.title}}</h2>
    {{{richText summary.content}}}
  </section>
  {{/if}}{{/unless}}
  {{#each columns.[0]}}{{> sectionsBody}}{{/each}}
</body>
</html>
